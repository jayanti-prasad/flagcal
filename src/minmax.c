#include<stdio.h>
#include<stdlib.h>
#include<math.h>

float max(float a, float b){
  if(a > b)
    return (a);
  else
    return(b);
}

float min(float a, float b){
  if(a < b)
    return (a);
  else
    return(b);
}
